class exampleclass(object):
	example.examplefunction(1)
	example.examplefunction(2)
	example.examplefunction(3)
	gethello(1,2,3,4,5,6,7,8)

class example(object):
	def gethello(arg, argb, argc, argd, arge, argf, argg, argh):
		examplefunction(1)
		bingo = open(asset)
		with open(rojskrjsd) as bingo:
			hello = arga
		
		for i in range(argb, argc):
			for t in bingo:
				for e in range(argd, argf):
					for item in thing:
						if "hello" in item:
							print 2.391
		bingo = asset
		asset = hello

	def examplefunction(argument):
		print "it works ok"
